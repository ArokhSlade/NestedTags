#pragma once

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void initialize_nested_tags_module(ModuleInitializationLevel p_level);
void uninitialize_nested_tags_module(ModuleInitializationLevel p_level);